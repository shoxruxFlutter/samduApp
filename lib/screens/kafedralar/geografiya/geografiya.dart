import 'package:flutter/material.dart';

class Geografiya extends StatelessWidget {
  const Geografiya({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Geografiya kafedrasi"),
        backgroundColor: Colors.blue,
      ),
    );
  }}