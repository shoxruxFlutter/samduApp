import 'package:flutter/material.dart';

class TeacherScreenWidget extends StatelessWidget {
  const TeacherScreenWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Teacher'),
      ),
    );
  }
}
