import 'package:flutter/material.dart';

class AnketaScreenWidget extends StatelessWidget {
  const AnketaScreenWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
